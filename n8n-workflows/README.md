# n8n Workflows

Exported n8n workflow JSON files for version control.

- **workflow-2-awb-status-check.json** — (To add) AWB Status Check: fetches AWBs due for check, calls backend track API, updates history and latest status.

Import these in the n8n editor (e.g. via Caddy at `/n8n`) when the n8n service is running.
