/** Bump only when persisted milestone projection semantics change (legs/flows/steps algorithms). */
export const MILESTONE_PROJECTION_VERSION = "1.0.3";

/** Bump when query scheduling intervals, halt/retry thresholds, attention bucketing semantics change (separate bounded context). */
export const SCHEDULE_POLICY_VERSION = "1.0.0";
